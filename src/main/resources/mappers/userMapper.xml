<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.junhyun.boardwas.mapper.UserProfileMapper">

    <select id="getUserProfile" resultType="com.junhyun.boardwas.dto.UserDto">
        SELECT id,
        email,
        password,
        nickname,
        isAdmin,
        createTime,
        isWithDraw,
        status,
        updateTime
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="register" parameterType="com.junhyun.boardwas.dto.UserDto">
        INSERT INTO user (email, password, nickname,
            isAdmin, isWithDraw, status, createTime, updateTime)
        VALUES(#{email}, #{password}, #{nickname},
            #{isAdmin}, #{isWithDraw}, #{status},
            #{createTime}, #{updateTime})
    </insert>
    <insert id="insertUserProfile" parameterType="com.junhyun.boardwas.dto.UserDto">
        INSERT INTO user (email, password, nickname,
        isAdmin, isWithDraw, status,
        createTime, updateTime)
        VALUES(#{email}, #{password}, #{nickname},
        #{isAdmin}, #{isWithDraw}, #{status},
        #{createTime}, #{updateTime})
    </insert>
    <update id="updateUserProfile" parameterType="com.junhyun.boardwas.dto.UserDto">
        UPDATE user
        SET password = #{password}
        nickname = #{nickname}
        isWithDraw = #{isWithDraw}
        status = #{status}
        WHERE id = #{id}
    </update>
    <delete id="deleteUserProfile">
        DELETE
        FROM user
        WHERE id = #{id}
    </delete>
    <select id="findByEmailAndPassword" resultType="com.junhyun.boardwas.dto.UserDto">
        SELECT id,
        email,
        password,
        nickname,
        isAdmin,
        createTime,
        isWithDraw,
        status,
        updateTime
        FROM user
        WHERE email = #{email}
        AND password = #{password}
        AND status != 'DELETE'
    </select>

    <select id="idCheck" resultType="int">
        SELECT COUNT(id)
        FROM user
        WHERE email = #{email}
    </select>

    <update id="updatePassword" parameterType="com.junhyun.boardwas.dto.UserDto">
        UPDATE user
        SET password = #{password}
        WHERE email = #{email}
    </update>

</mapper>